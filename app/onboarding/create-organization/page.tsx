export default function CreateOrganizationPage() {
  return (
    <main className="shell">
      <h1>Create Organization</h1>
      <p className="muted">
        Server action available: createOrganization. The form should collect name, slug, timezone, currency, business type,
        payroll cycle, and financial period.
      </p>
    </main>
  )
}
